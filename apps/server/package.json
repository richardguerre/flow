{
  "name": "@flowdev/server",
  "description": "This is the server of Flow",
  "private": true,
  "scripts": {
    "print-schema": "bun run gen && NODE_ENV=development bun run src/graphql/index.ts",
    "dev": "NODE_ENV=development bun --watch run src/index.ts",
    "type-check": "bun run gen && tsc --noEmit && echo '✅ Type checking passed!'",
    "build": "bun build ./src/index.ts --outdir dist --target bun",
    "copy-web": "rm -rf ./dist/web && cp -r ../web/dist ./dist/web && echo '✅ apps/web/dist copied to apps/server/dist/web'",
    "move-prisma-binary": "mv dist/client/* dist || echo '🔵 Ignore above error. Prisma binary was already moved.' && rm -r dist/client || echo '🔵 Ignore above error. dist/client was already removed.' && echo '✅ Prisma binary successfully moved from dist/client to dist'",
    "post-build": "bun run copy-web && bun run move-prisma-binary",
    "build:prod": "bun run build && bun run post-build && echo '✅ Successfully built for prod!",
    "start": "bun run build -W && bun run get-web && cd dist && node index.js",
    "test": "vitest --config .vitest/config.ts",
    "gen": "bunx prisma generate && echo '✅ Prisma client successfully generated!'",
    "db:dev": "bunx prisma migrate dev",
    "db:migrate": "bunx prisma migrate dev --create-only",
    "db:reset": "bun run prisma/dropOtherSchemas && prisma migrate reset --force",
    "db:seed": "bun run prisma/seed.ts",
    "setup:db": "psql -c \"CREATE DATABASE flow;\" || echo \"🔵 Ignore the above error. Everything is good to go!\"",
    "setup:db:test": "psql -c \"CREATE DATABASE flow_test;\" || echo \"🔵 Ignore the above error. Everything is good to go!\"",
    "env:example": "cp -n .env.example .env || true",
    "env:codespaces": "cp .env.codespaces .env"
  },
  "prisma": {
    "seed": "bun run prisma/seed.ts"
  },
  "devDependencies": {
    "@types/bcryptjs": "2.4.2",
    "@types/chance": "1.1.3",
    "@types/express": "4.17.13",
    "@types/node": "18.7.16",
    "@types/supertest": "2.0.12",
    "@vercel/ncc": "0.36.1",
    "camel-case": "4.1.2",
    "chance": "1.1.8",
    "prisma": "5.4.1",
    "supertest": "6.2.4",
    "vite-plugin-checker": "0.5.1",
    "vitest": "0.23.4"
  },
  "dependencies": {
    "@elysiajs/static": "0.7.1",
    "@flowdev/nearest-color": "workspace:*",
    "@pothos/core": "3.30.0",
    "@pothos/plugin-prisma": "3.61.0",
    "@pothos/plugin-prisma-utils": "0.11.0",
    "@pothos/plugin-relay": "3.27.0",
    "@pothos/plugin-scope-auth": "3.20.0",
    "@pothos/plugin-simple-objects": "3.7.0",
    "@pothos/plugin-smart-subscriptions": "3.7.1",
    "@pothos/plugin-with-input": "3.8.0",
    "@prisma/client": "5.4.1",
    "bcryptjs": "2.4.3",
    "dayjs": "1.11.7",
    "elysia": "0.7.15",
    "express": "4.18.1",
    "graphql": "16.6.0",
    "graphql-scalars": "1.18.0",
    "graphql-yoga": "4.0.5",
    "pg-boss": "9.0.3"
  }
}
