{
  "name": "@flowdev/essentials",
  "description": "The official and default plugin for Flow containing essential features such as a morning routine and a shutdown routine.",
  "version": "0.0.1",
  "private": false,
  "scripts": {
    "build:web": "vite build -c vite.web.ts",
    "build:server": "vite build -c vite.server.ts",
    "build": "npm run build:web && npm run build:server"
  },
  "dependencies": {
    "@flowdev/plugin": "*"
  },
  "devDependencies": {
    "@types/react": "^18.0.26",
    "vite": "^4.0.0"
  }
}
