{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "env:codespaces": {
      "outputs": ["./.env"]
    },
    "dev": {
      "cache": false
    },
    "seed": {
      "cache": false
    },
    "build": {
      "outputs": ["./dist"]
    }
  }
}
