{
  "name": "backend",
  "description": "This is the backend of Persama",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "NODE_ENV=development ts-node-dev --transpile-only --exit-child --respawn src/index.ts",
    "start": "ts-node --transpile-only src/index.ts"
  },
  "devDependencies": {
    "@types/node": "^18.7.16",
    "ts-node": "^10.9.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.8.3"
  },
  "dependencies": {
    "@graphql-yoga/node": "^2.13.13",
    "graphql": "^16.6.0",
    "nexus": "^1.3.0"
  }
}
